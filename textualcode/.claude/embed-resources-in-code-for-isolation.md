# embed-resources-in-code-for-isolation

When deploying agents with restricted I/O access, embed critical resources like prompts as code constants rather than configuration files to survive isolation boundaries.

_Category: Deployment_
