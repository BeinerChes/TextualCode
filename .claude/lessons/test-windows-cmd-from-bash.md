# test-windows-cmd-from-bash

Test Windows native executables launched from git-bash separately and account for MSYS2 path rewrites (e.g., /c → C:/) to catch silent failures in command execution or parameter passing.

_Category: Shell / Platform Integration_
