# prefer-solid-block-glyphs-for-banners

Use solid block glyphs (█) instead of thin box-drawing characters (╔╦╗═╝) for ASCII art banners in TUIs, preventing rendering gaps and visual degradation across different terminal fonts.

_Category: UI Design_
