# two-step-quit-with-confirm-window

Implement destructive actions (quit, delete, reset) as two-step confirms with ~3 second timeout: first action arms and shows 'Press again to confirm' inline; second press within window executes; timeout disarms silently.

_Category: UX_
