# session-config-cascades-with-exceptions

Session or context-level config (setting_sources, system_prompt, etc.) cascades to spawned subcomponents — set once at the session level and check the docs for subcomponent-specific exceptions.

_Category: SDK Configuration_
