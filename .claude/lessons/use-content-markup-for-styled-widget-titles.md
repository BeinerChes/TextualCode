# use-content-markup-for-styled-widget-titles

Pass styled Content objects (via Content.from_text with markup) as widget titles to render colored badges and metadata inline, rather than relying on plain string concatenation.

_Category: UI_
