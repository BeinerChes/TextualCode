# preserve-within-model-cost-granularity

When splitting cost across agents that may share a model, preserve per-model cost from model_usage and split only within a single model by token proportion, because per-token rates differ across models.

_Category: Cost Tracking_
