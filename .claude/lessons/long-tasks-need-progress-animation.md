# long-tasks-need-progress-animation

Display live animated progress (spinner + elapsed seconds) for any async task exceeding 5 seconds, or users perceive the UI as completely hung even when the backend is working correctly.

_Category: UX_
