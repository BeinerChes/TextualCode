# use-bypass-permissions-for-readonly-subagents

Set permission_mode='bypassPermissions' on read-only subagents to allow autonomous tool use without blocking on user confirmation prompts for each operation.

_Category: Permissions_
