# explain-uv-tool-isolation-in-docs

Document that `uv tool install` creates a launcher shim plus an isolated sandbox environment, not a frozen bundle, so users understand the dependency model and storage location.

_Category: Packaging_
