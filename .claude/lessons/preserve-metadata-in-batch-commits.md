# preserve-metadata-in-batch-commits

When committing batch review/fix results, use pathspec or explicit file staging to include only reviewed code changes and never commit session metadata files (state, lessons, transcripts) which are harvester-owned.

_Category: Workflow_
