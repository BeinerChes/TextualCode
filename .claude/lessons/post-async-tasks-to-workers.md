# post-async-tasks-to-workers

Post UI requests as messages to background workers instead of calling them directly to keep the UI responsive during long-running async operations.

_Category: Architecture_
