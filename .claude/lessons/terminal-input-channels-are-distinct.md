# terminal-input-channels-are-distinct

Treat drag-and-drop and Ctrl+V as separate input channels with different delivery paths; terminal emulator behavior varies by OS and version, so test both paths independently instead of assuming they work identically.

_Category: Terminals_
