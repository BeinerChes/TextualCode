# prefer-rich-pretty-textual-widget

Use Textual's Pretty widget to render Python objects with syntax coloring and structure-aware indentation instead of json.dumps() + Static, preventing hard-to-read walls of escaped JSON text.

_Category: UI_
