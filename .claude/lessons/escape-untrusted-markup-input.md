# escape-untrusted-markup-input

Escape model-controlled or external tool output with rich.markup.escape before passing to markup-accepting widgets (e.g., Collapsible titles) to prevent CWE-150 markup injection and MarkupError crashes.

_Category: Security_
