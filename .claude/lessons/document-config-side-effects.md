# document-config-side-effects

When enabling new config sources (filesystem settings, permissions, etc.), verify and document all side effects that change behavior beyond the intended feature (e.g., auto-approval of tools via permissions.allow rules).

_Category: SDK Configuration_
