[project]
name = "textualcode"
version = "0.1.0"
description = "A stub Claude-Code-style terminal UI built with Textual"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.2.88",
    "textual>=0.80",
]

[tool.uv]
package = false
